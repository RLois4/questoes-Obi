#include <bits/stdc++.h>
using namespace std;

inline int manhat(int ax, int ay, int bx, int by) {
  return abs(ax - bx) + abs(ay - by);
}

int closestPt(pair<pair<int,int>,pair<int,int>> a, pair<pair<int,int>,pair<int,int>> b) {
  
}

int main() {
  int xi, yi, xf, yf;
  cin >> xi >> yi >> xf >> yf;
  int n; cin >> n;
  for(int i=0; i<n; i++) {
    int ax, ay, bx, by;
    cin >> ax >> ay >> bx >> by;
  }
}